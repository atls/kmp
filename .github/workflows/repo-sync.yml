name: Repository synchronization

on:
  pull_request:
  # TODO uncomment
  #   types:
  #     - closed
  #   branches:
  #     - master
  # workflow_dispatch:

jobs:
  prepare:
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      replacements: ${{ steps.getReplacementsStep.outputs.replacements }}
    steps:
      # TODO uncomment
      # - uses: atls/shared/.github/actions/sync/read-replacements@master
      - uses: atls/shared/.github/actions/sync/read-replacements@feat/repo-sync
        id: getReplacementsStep

  repoSync:
    needs: prepare
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    # uses: atls/shared/.github/workflows/reusable-repo-sync.yml@master
    uses: atls/shared/.github/workflows/repo-sync.yaml@feat/repo-sync
    secrets:
      appSecret: ${{ secrets.PERSONACLICK_COURIER_SECRET }}
    with:
      appId: ${{ vars.PERSONACLICK_COURIER_ID }}
      replacements: ${{ needs.prepare.outputs.replacements }}
      repositoryOwner: rees46
      reviewerUsername: iwwwanow
      targetRepository: rees46/kmp
